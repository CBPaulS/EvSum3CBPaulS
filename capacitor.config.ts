import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Sumativa3_CBPaulS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
